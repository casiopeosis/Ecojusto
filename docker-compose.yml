version: "3.9"
services:
  app:
    build: .
    ports:
      - "8501:8501"
    environment:
      - PYTHONPATH=/app
    volumes:
      - ./src:/app/src        # hot-reload en desarrollo
      - ./frontend:/app/frontend