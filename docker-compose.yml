version: "3.9"

services:
  ecojusto:
    build: .
    ports:
      - "8501:8501"
    env_file:
      - .env
    volumes:
      # Hot-reload: cambios en el código se reflejan sin rebuild
      - .:/app
    restart: unless-stopped
