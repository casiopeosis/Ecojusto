# Renombra este archivo a .env
# Solo necesario si quieres activar el módulo de narrativa LLM.
# El resto del sistema (Banco Mundial, algoritmos) no requiere ninguna key.
ANTHROPIC_API_KEY=sk-ant-...
